<UserControl x:Class="RouteOptimizer.Dispatcher.Wpf.Views.DeliveryHistoryPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid>
        <TextBlock Text="Loading delivery history..."
                   Foreground="{StaticResource TextSecondaryBrush}"
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"/>

        <TextBlock Text="{Binding ErrorMessage}"
                   Foreground="{StaticResource DangerBrush}"
                   TextWrapping="Wrap" TextAlignment="Center" Margin="16"
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   Visibility="{Binding HasError, Converter={StaticResource BoolToVisibilityConverter}}"/>

        <TextBlock Text="No delivery attempts recorded for this order yet."
                   Foreground="{StaticResource TextSecondaryBrush}"
                   TextWrapping="Wrap" TextAlignment="Center" Margin="16"
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVisibilityConverter}}"/>

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Attempts}" Margin="0,0,4,0">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{StaticResource SurfaceBrush}"
                                BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                                CornerRadius="10" Padding="14,12" Margin="0,0,0,10">
                            <StackPanel>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{Binding OutcomeDisplay}" FontWeight="SemiBold">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="#16A34A"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsFailed}" Value="True">
                                                        <Setter Property="Foreground" Value="{StaticResource DangerBrush}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                    <TextBlock Grid.Column="1" Text="{Binding AttemptedAtDisplay}"
                                               Foreground="{StaticResource TextSecondaryBrush}"/>
                                </Grid>

                                <StackPanel Orientation="Horizontal" Margin="0,8,0,0"
                                            Visibility="{Binding IsFailed, Converter={StaticResource BoolToVisibilityConverter}}">
                                    <TextBlock Text="Reason" Style="{StaticResource CaptionTextStyle}" Margin="0,0,8,0"/>
                                    <TextBlock Text="{Binding FailureReasonDisplay}"/>
                                </StackPanel>

                                <StackPanel Margin="0,6,0,0">
                                    <TextBlock Text="Notes" Style="{StaticResource CaptionTextStyle}"/>
                                    <TextBlock Text="{Binding NotesDisplay}" TextWrapping="Wrap"/>
                                </StackPanel>

                                <Border Margin="0,10,0,0" CornerRadius="8" ClipToBounds="True"
                                        HorizontalAlignment="Left"
                                        Visibility="{Binding HasPhoto, Converter={StaticResource BoolToVisibilityConverter}}">
                                    <Image Source="{Binding PhotoUrl}" MaxHeight="220" Stretch="Uniform"
                                           Cursor="Hand" ToolTip="Click to open full size"
                                           MouseLeftButtonUp="Photo_MouseLeftButtonUp"/>
                                </Border>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
