<UserControl x:Class="RouteOptimizer.Dispatcher.Wpf.Views.VehiclesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <Border Width="4" Height="26" CornerRadius="2"
                        Background="{StaticResource PrimaryBrush}" Margin="0,0,12,0"/>
                <StackPanel>
                    <TextBlock Text="Vehicles" Style="{StaticResource HeaderTextStyle}"/>
                    <TextBlock Text="Fleet per warehouse" Style="{StaticResource CaptionTextStyle}"/>
                </StackPanel>
            </StackPanel>

            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Warehouse" Style="{StaticResource CaptionTextStyle}"
                           Margin="0,0,8,0"/>
                <ComboBox Width="220"
                          ItemsSource="{Binding Warehouses}"
                          SelectedItem="{Binding SelectedWarehouse}"
                          DisplayMemberPath="Name"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <Button Content="Refresh" Style="{StaticResource SecondaryButtonStyle}"
                        Command="{Binding LoadVehiclesCommand}" Margin="0,0,8,0"/>
                <Button Content="Edit" Style="{StaticResource SecondaryButtonStyle}"
                        Command="{Binding EditVehicleCommand}"
                        IsEnabled="{Binding HasSelectedVehicle}" Margin="0,0,8,0"/>
                <Button Content="Delete" Style="{StaticResource DangerButtonStyle}"
                        Command="{Binding DeleteVehicleCommand}"
                        IsEnabled="{Binding HasSelectedVehicle}"/>
            </StackPanel>

            <TextBlock Grid.Column="1" Text="Loading..." Margin="16,0,0,0"
                       Foreground="{StaticResource TextSecondaryBrush}"
                       VerticalAlignment="Center" HorizontalAlignment="Left"
                       Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"/>

            <Button Grid.Column="2" Content="+ Create"
                    Command="{Binding CreateVehicleCommand}"
                    IsEnabled="{Binding HasSelectedWarehouse}"/>
        </Grid>

        <Border Grid.Row="2" Style="{StaticResource CardBorderStyle}" Padding="1">
            <Grid>
            <DataGrid ItemsSource="{Binding Vehicles}"
                      SelectedItem="{Binding SelectedVehicle}"
                      IsReadOnly="True"
                      BorderThickness="0"
                      Background="Transparent">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="180"/>
                    <DataGridTextColumn Header="Max Weight (kg)" Binding="{Binding MaxWeightKg, StringFormat=F1}" Width="140"/>
                    <DataGridTextColumn Header="Max Volume (m³)" Binding="{Binding MaxVolumeM3, StringFormat=F1}" Width="140"/>
                    <DataGridTextColumn Header="License" Binding="{Binding LicenseCategory}" Width="100"/>
                    <DataGridTextColumn Header="Cargo Types" Binding="{Binding AllowedCargoTypesDisplay}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
                <TextBlock Text="No vehicles in this warehouse yet."
                           HorizontalAlignment="Center" VerticalAlignment="Center"
                           TextWrapping="Wrap" TextAlignment="Center" Margin="16"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVisibilityConverter}}"/>
            </Grid>
        </Border>

        <TextBlock Grid.Row="3"
                   Text="{Binding ErrorMessage}"
                   Foreground="{StaticResource DangerBrush}"
                   TextWrapping="Wrap"
                   Margin="0,12,0,0"
                   Visibility="{Binding HasError, Converter={StaticResource BoolToVisibilityConverter}}"/>
    </Grid>
</UserControl>
