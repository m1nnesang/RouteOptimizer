<UserControl x:Class="RouteOptimizer.Dispatcher.Wpf.Views.VehiclesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Vehicles" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
            <TextBlock Text="Warehouse:" Margin="16,0,4,0" VerticalAlignment="Center"/>
            <ComboBox Width="220" VerticalAlignment="Center"
                      ItemsSource="{Binding Warehouses}"
                      SelectedItem="{Binding SelectedWarehouse}"
                      DisplayMemberPath="Name"/>
            <TextBlock Text="Loading..." Margin="16,0,0,0" VerticalAlignment="Center"
                       Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="Refresh" Command="{Binding LoadVehiclesCommand}" Padding="12,4"/>
            <Button Content="Create" Command="{Binding CreateVehicleCommand}"
                    IsEnabled="{Binding HasSelectedWarehouse}" Margin="8,0,0,0" Padding="12,4"/>
            <Button Content="Edit" Command="{Binding EditVehicleCommand}"
                    IsEnabled="{Binding HasSelectedVehicle}" Margin="8,0,0,0" Padding="12,4"/>
            <Button Content="Delete" Command="{Binding DeleteVehicleCommand}"
                    IsEnabled="{Binding HasSelectedVehicle}" Margin="8,0,0,0" Padding="12,4"/>
        </StackPanel>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Vehicles}"
                  SelectedItem="{Binding SelectedVehicle}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="160"/>
                <DataGridTextColumn Header="Max Weight (kg)" Binding="{Binding MaxWeightKg, StringFormat=F1}" Width="120"/>
                <DataGridTextColumn Header="Max Volume (m³)" Binding="{Binding MaxVolumeM3, StringFormat=F1}" Width="120"/>
                <DataGridTextColumn Header="License" Binding="{Binding LicenseCategory}" Width="90"/>
                <DataGridTextColumn Header="Cargo Types" Binding="{Binding AllowedCargoTypesDisplay}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="3"
                   Text="{Binding ErrorMessage}"
                   Foreground="Red"
                   Margin="0,8,0,0"
                   Visibility="{Binding HasError, Converter={StaticResource BoolToVisibilityConverter}}"/>
    </Grid>
</UserControl>
