<UserControl x:Class="RouteOptimizer.Dispatcher.Wpf.Views.OrdersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Заказы" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
            <Button Content="Обновить"
                    Command="{Binding LoadOrdersCommand}"
                    Margin="16,0,0,0" Padding="12,4"/>
            <TextBlock Text="Загрузка..." Margin="16,0,0,0" VerticalAlignment="Center"
                       Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"/>
        </StackPanel>

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Orders}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="100"/>
                <DataGridTextColumn Header="Тип" Binding="{Binding OrderType}" Width="100"/>
                <DataGridTextColumn Header="Город" Binding="{Binding City}" Width="120"/>
                <DataGridTextColumn Header="Улица" Binding="{Binding Street}" Width="180"/>
                <DataGridTextColumn Header="Груз" Binding="{Binding CargoType}" Width="100"/>
                <DataGridTextColumn Header="Вес (кг)" Binding="{Binding Weight}" Width="80"/>
                <DataGridTextColumn Header="Телефон" Binding="{Binding PhoneNumber}" Width="120"/>
                <DataGridTextColumn Header="Дата" Binding="{Binding CreatedAt, StringFormat=dd.MM.yyyy}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
