<UserControl x:Class="RouteOptimizer.Dispatcher.Wpf.Views.OrdersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:RouteOptimizer.Dispatcher.Wpf.Views">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <Border Width="4" Height="26" CornerRadius="2"
                        Background="{StaticResource PrimaryBrush}" Margin="0,0,12,0"/>
                <StackPanel>
                    <TextBlock Text="Orders" Style="{StaticResource HeaderTextStyle}"/>
                    <TextBlock Text="All customer orders" Style="{StaticResource CaptionTextStyle}"/>
                </StackPanel>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="24,0,0,0" VerticalAlignment="Center">
                <TextBox Width="200" VerticalAlignment="Center"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged, Delay=200}"
                         ToolTip="Search by city, street, recipient, phone..."/>
                <TextBlock Text="Status" Style="{StaticResource CaptionTextStyle}"
                           VerticalAlignment="Center" Margin="16,0,8,0"/>
                <ComboBox ItemsSource="{Binding StatusFilters}"
                          SelectedItem="{Binding SelectedStatusFilter}"
                          Width="140"/>
                <TextBlock Text="Date" Style="{StaticResource CaptionTextStyle}"
                           VerticalAlignment="Center" Margin="16,0,8,0"/>
                <DatePicker SelectedDate="{Binding SelectedDate}" Width="140"/>
                <Button Content="✕" Width="28" Margin="4,0,0,0"
                        Style="{StaticResource SecondaryButtonStyle}"
                        ToolTip="Show all days"
                        Command="{Binding ClearDateFilterCommand}"
                        Visibility="{Binding HasDateFilter, Converter={StaticResource BoolToVisibilityConverter}}"/>
                <TextBlock Text="Loading..." Margin="16,0,0,0"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           VerticalAlignment="Center"
                           Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"/>
            </StackPanel>

            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <Button Content="Refresh" Margin="0,0,8,0"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Command="{Binding LoadOrdersCommand}"/>
                <Button Content="+ Create order"
                        Command="{Binding CreateOrderCommand}"/>
            </StackPanel>
        </Grid>

        <TextBlock Grid.Row="1"
                   Text="{Binding ErrorMessage}"
                   Foreground="{StaticResource DangerBrush}"
                   TextWrapping="Wrap"
                   Margin="0,0,0,12"
                   Visibility="{Binding HasError, Converter={StaticResource BoolToVisibilityConverter}}"/>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="440"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource CardBorderStyle}" Padding="1">
                <Grid>
                <DataGrid ItemsSource="{Binding Orders}"
                          SelectedItem="{Binding SelectedOrder}"
                          IsReadOnly="True"
                          BorderThickness="0"
                          Background="Transparent"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <DataGrid.Columns>
                        <DataGridTemplateColumn Header="Status" Width="150">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource StatusChipStyle}"
                                            Background="{Binding Status, Converter={StaticResource StatusChipBgConverter}}">
                                        <TextBlock Text="{Binding Status}"
                                                   Style="{StaticResource StatusChipTextStyle}"
                                                   Foreground="{Binding Status, Converter={StaticResource StatusChipFgConverter}}"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="Type" Binding="{Binding OrderType}" Width="110"/>
                        <DataGridTextColumn Header="City" Binding="{Binding City}" Width="130"/>
                        <DataGridTextColumn Header="Street" Binding="{Binding Street}" Width="200">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="{StaticResource TextLinkBrush}"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Cargo" Binding="{Binding CargoType}" Width="110"/>
                        <DataGridTextColumn Header="Weight (kg)" Binding="{Binding Weight}" Width="100"/>
                        <DataGridTextColumn Header="Phone" Binding="{Binding PhoneNumber}" Width="140"/>
                        <DataGridTextColumn Header="Date" Binding="{Binding CreatedAt, StringFormat=dd.MM.yyyy}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
                    <TextBlock Text="No orders for the selected period."
                               HorizontalAlignment="Center" VerticalAlignment="Center"
                               TextWrapping="Wrap" TextAlignment="Center" Margin="16"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVisibilityConverter}}"/>
                </Grid>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource CardBorderStyle}" Padding="16">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Margin="0,0,0,12">
                        <TextBlock Text="Delivery history" Style="{StaticResource HeaderTextStyle}"/>
                        <TextBlock Text="Attempts, failures and proof photos" Style="{StaticResource CaptionTextStyle}"/>
                    </StackPanel>

                    <TextBlock Grid.Row="1" Text="Select an order to see its delivery history"
                               HorizontalAlignment="Center" VerticalAlignment="Center"
                               TextWrapping="Wrap" TextAlignment="Center"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               Visibility="{Binding NoOrderSelected, Converter={StaticResource BoolToVisibilityConverter}}"/>

                    <views:DeliveryHistoryPanel Grid.Row="1"
                                                DataContext="{Binding DeliveryHistory}"
                                                Visibility="{Binding DataContext.HasSelectedOrder, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BoolToVisibilityConverter}}"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
