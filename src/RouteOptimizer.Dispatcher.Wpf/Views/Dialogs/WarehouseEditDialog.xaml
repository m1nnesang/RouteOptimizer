<Window x:Class="RouteOptimizer.Dispatcher.Wpf.Views.Dialogs.WarehouseEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}"
        Width="440" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding Title}"
                   Style="{StaticResource HeaderTextStyle}" FontSize="18" Margin="0,0,0,16"/>

        <StackPanel Grid.Row="1">
            <TextBlock Text="Name" Style="{StaticResource CaptionTextStyle}" Margin="0,0,0,4"/>
            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12"/>

            <TextBlock Text="City" Style="{StaticResource CaptionTextStyle}" Margin="0,0,0,4"/>
            <TextBox Text="{Binding City, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12"/>

            <TextBlock Text="Street" Style="{StaticResource CaptionTextStyle}" Margin="0,0,0,4"/>
            <TextBox Text="{Binding Street, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12"/>

            <TextBlock Text="Postal code" Style="{StaticResource CaptionTextStyle}" Margin="0,0,0,4"/>
            <TextBox Text="{Binding PostalCode, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12"/>

            <TextBlock Text="Country" Style="{StaticResource CaptionTextStyle}" Margin="0,0,0,4"/>
            <TextBox Text="{Binding Country, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12"/>

            <TextBlock Text="Coordinates are filled automatically from the address."
                       Style="{StaticResource CaptionTextStyle}" TextWrapping="Wrap"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Cancel" Width="92" Margin="0,0,8,0"
                    Style="{StaticResource SecondaryButtonStyle}" Click="CancelButton_Click"/>
            <Button Content="Save" Width="92"
                    IsEnabled="{Binding CanConfirm}" Click="ConfirmButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
