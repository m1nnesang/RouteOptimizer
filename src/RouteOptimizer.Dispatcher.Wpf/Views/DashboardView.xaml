<UserControl x:Class="RouteOptimizer.Dispatcher.Wpf.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <Border Width="4" Height="26" CornerRadius="2"
                        Background="{StaticResource PrimaryBrush}" Margin="0,0,12,0"/>
                <TextBlock Text="Dashboard" Style="{StaticResource HeaderTextStyle}"/>
            </StackPanel>

            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <TextBlock Text="Date" VerticalAlignment="Center" Margin="0,0,8,0"
                           Style="{StaticResource CaptionTextStyle}"/>
                <DatePicker SelectedDate="{Binding SelectedDate}" Width="150"/>
            </StackPanel>
        </Grid>

        <TextBlock Grid.Row="1" Text="{Binding ErrorMessage}"
                   Foreground="{StaticResource DangerBrush}" TextWrapping="Wrap"
                   Margin="0,0,0,12"/>

        <WrapPanel Grid.Row="2" Margin="0,0,0,20">
            <Border Style="{StaticResource CardBorderStyle}" Width="120" Margin="0,0,12,12">
                <StackPanel>
                    <TextBlock Text="Total stops" Style="{StaticResource CaptionTextStyle}"/>
                    <TextBlock Text="{Binding Stops.Total}" FontSize="26" FontWeight="Bold"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource CardBorderStyle}" Width="120" Margin="0,0,12,12">
                <StackPanel>
                    <TextBlock Text="Completed" Style="{StaticResource CaptionTextStyle}"/>
                    <TextBlock Text="{Binding Stops.Completed}" FontSize="26" FontWeight="Bold"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource CardBorderStyle}" Width="120" Margin="0,0,12,12">
                <StackPanel>
                    <TextBlock Text="Partial" Style="{StaticResource CaptionTextStyle}"/>
                    <TextBlock Text="{Binding Stops.PartiallyCompleted}" FontSize="26" FontWeight="Bold"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource CardBorderStyle}" Width="120" Margin="0,0,12,12">
                <StackPanel>
                    <TextBlock Text="Failed" Style="{StaticResource CaptionTextStyle}"/>
                    <TextBlock Text="{Binding Stops.Failed}" FontSize="26" FontWeight="Bold"
                               Foreground="{StaticResource DangerBrush}"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource CardBorderStyle}" Width="120" Margin="0,0,12,12">
                <StackPanel>
                    <TextBlock Text="Pending" Style="{StaticResource CaptionTextStyle}"/>
                    <TextBlock Text="{Binding Stops.Pending}" FontSize="26" FontWeight="Bold"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource CardBorderStyle}" Width="120" Margin="0,0,12,12">
                <StackPanel>
                    <TextBlock Text="Skipped" Style="{StaticResource CaptionTextStyle}"/>
                    <TextBlock Text="{Binding Stops.Skipped}" FontSize="26" FontWeight="Bold"/>
                </StackPanel>
            </Border>
        </WrapPanel>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <DockPanel Grid.Column="0">
                <TextBlock DockPanel.Dock="Top" Text="Failed deliveries by reason"
                           FontWeight="SemiBold" Margin="0,0,0,8"/>
                <DataGrid ItemsSource="{Binding FailureBreakdown}" AutoGenerateColumns="False"
                          IsReadOnly="True" HeadersVisibility="Column">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Reason" Binding="{Binding Reason}" Width="*"/>
                        <DataGridTextColumn Header="Count" Binding="{Binding Count}" Width="Auto"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>

            <DockPanel Grid.Column="2">
                <TextBlock DockPanel.Dock="Top" Text="Driver performance"
                           FontWeight="SemiBold" Margin="0,0,0,8"/>
                <DataGrid ItemsSource="{Binding DriverPerformance}" AutoGenerateColumns="False"
                          IsReadOnly="True" HeadersVisibility="Column">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Driver" Binding="{Binding DriverName}" Width="*"/>
                        <DataGridTextColumn Header="Delivered" Binding="{Binding Delivered}" Width="Auto"/>
                        <DataGridTextColumn Header="Failed" Binding="{Binding Failed}" Width="Auto"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Grid>
    </Grid>
</UserControl>
